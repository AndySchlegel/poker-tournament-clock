{
  "name": "poker-tournament-clock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Premium browser-based poker tournament clock with configurable levels, players, breaks, and TV display.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run"
  },
  "dependencies": {
    "@vitejs/plugin-basic-ssl": "latest",
    "vite": "latest",
    "vitest": "latest"
  },
  "devDependencies": {}
}
